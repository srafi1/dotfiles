if [ $# -eq 0 ]
then
    echo "Usage: school-proxy [on|off|show]"
    exit
fi

if [ $1 = on ]
then
    input=proxy-on.in
elif [ $1 = off ]
then
    input=proxy-off.in
else
    input=proxy-show.in
fi

cd ~/github/ProxyMan/
./main.sh < $input

